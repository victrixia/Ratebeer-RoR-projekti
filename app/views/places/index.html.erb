<p id="notice"><%= notice %></p>

<%= form_tag places_path do %>
    city <%= text_field_tag :city, params[:city] %>
    <%= submit_tag "Search" %>
<% end %>

<% if @places %>
    <table class  ="table table-hover">
      <thead>
      <td>name</td>
      <% Place.rendered_fields.each do |f| %>
          <td><%= f %></td>
      <% end %>
      </thead>
      <% @places.each do |place| %>
          <tr>
            <td><%= place.name%></td>
            <% Place.rendered_fields.each do |f| %>
                <td><%= place.send(f) %></td>

            <% end %>
            <td><%= link_to 'Restaurant information', place_path(place.id), class: 'btn btn-info' %></td>
          </tr>
      <% end %>
    </table>
<% end %>